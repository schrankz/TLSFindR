% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_niche_scoring.R
\name{run_niche_scoring}
\alias{run_niche_scoring}
\title{Score niches using signatures via GSVA/ssGSEA}
\usage{
run_niche_scoring(
  seurat_obj,
  signatures,
  assay = c("Xenium", "SCT"),
  slot = "data",
  make_plot = TRUE,
  plot_type = c("bar", "dot"),
  plot_title = "TLS Signatures Per Niche",
  niche_label = "niches",
  labeled = FALSE,
  palette = NULL
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{signatures}{A named list of gene vectors (signatures).}

\item{assay}{Character. Which assay to use (e.g., "Xenium" or "SCT").}

\item{slot}{Character. Slot to pull from (default "data").}

\item{make_plot}{Logical. Whether to return a ggplot2 object (bar or dot plot) depicting scores across niches.}

\item{plot_type}{Character. Type of plot to generate.
Either "bar" or "dot". Default is "bar".}

\item{plot_title}{Character. Title for plot.}

\item{niche_label}{Character. Metadata column with niche assignments. Default "niches".}

\item{labeled}{Logical. Add text labels to points in plot.}

\item{palette}{Optional named character vector of colors to use for niches.
If NULL (default), ggplot2 default discrete colors are used.
Named vectors are recommended to ensure correct color mapping.}
}
\value{
A list with \code{ssgsea_results} (data.frame) and \code{plot} (ggplot or NULL).
}
\description{
Compute enrichment scores (ssGSEA/GSVA) for provided gene signatures on niche-aggregated expression profiles and generate either a bar or dot plot depicting scores per niche.
}
\examples{
\dontrun{
out <- run_niche_scoring(obj, human_tls_signatures, assay = "SCT", slot = "data")
out$plot
}

}
